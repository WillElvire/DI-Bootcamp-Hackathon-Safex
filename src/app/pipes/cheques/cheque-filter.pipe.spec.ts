import { ChequeFilterPipe } from './cheque-filter.pipe';

describe('ChequeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChequeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
